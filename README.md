# Birthday Surprise (Vite + React + Tailwind)

## Develop
```bash
npm install
npm run dev
```

## Build
```bash
npm run build
```

Build output will be in `dist/`.

## Deploy on Cloudflare Pages
- Framework preset: **None**
- Build command: **npm run build**
- Build output directory: **dist**
